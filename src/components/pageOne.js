import React from "react";
import AppBar from "./appBar";
import Map from "./map";

const PageOne = () => {
  return (
    <div>
      <AppBar></AppBar>
      <Map></Map>
    </div>
  );
};

export default PageOne;
